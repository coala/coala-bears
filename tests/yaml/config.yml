extends:
    default

rules:
    # 80 chars should be enough, but don't fail if a line is longer
    line-length:
        max: 80
        level: warning
  
    colons:
      max-spaces-after: -1

    # don't bother me with this rule
    indentation: disable
